{
  "name": "ryohu",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "rm -rf public && NODE_ENV=development gulp watch",
    "build:production": "rm -rf public && NODE_ENV=production gulp build",
    "build:staging": "rm -rf public && NODE_ENV=staging gulp build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.24.7",
    "gulp": "^3.9.1",
    "gulp-data": "^1.3.1",
    "gulp-plumber": "^1.2.0",
    "gulp-postcss": "^7.0.1",
    "gulp-pug": "^3.3.0",
    "gulp-sass": "^3.2.1",
    "gulp-webserver": "^0.9.1",
    "postcss-cssnext": "^2.11.0",
    "postcss-flexbugs-fixes": "^3.3.1"
  }
}
